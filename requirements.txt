# LTX-2 Easy Prompt By LoRa-Daddy
# Requirements for both the Easy Prompt node and Vision Describe node
#
# Install with:
#   pip install -r requirements.txt
#
# Note: torch should already be installed by ComfyUI.
# If not: https://pytorch.org/get-started/locally/

# ── Core (both nodes) ─────────────────────────────────────────────────────────
torch>=2.1.0                  # Should already be present via ComfyUI
transformers>=4.45.0          # Qwen2.5-VL requires 4.45+
numpy>=1.24.0
Pillow>=9.0.0
huggingface_hub>=0.20.0       # For snapshot_download (online mode)

# ── Vision Describe node only ─────────────────────────────────────────────────
qwen-vl-utils>=0.0.8          # Required for process_vision_info

# ── Easy Prompt node only ─────────────────────────────────────────────────────
accelerate>=0.26.0            # For device_map="auto" model loading

# ── Optional but recommended ──────────────────────────────────────────────────
bitsandbytes>=0.41.0          # Enables 4-bit/8-bit quantisation to save VRAM
                              # Not required but useful on smaller GPUs
